---
name: expert-footprint
description: 创作足迹追踪专家。追踪长篇创作中的文风一致性——同一段落或场景描写在30~50章内不应逐字重复。文风层面的重复比遣词造句更重要。hentai.md亲密场景可例外。配合expert-memory（逻辑）+ expert-pacing（节奏）使用。
author: "Skills Pack v2"
tags: [novel, footprint, deduplication, mechanical-error, repetitive-writing, 创作, 去重, 防模板]
platforms: [番茄, 起点, 通用]
version: 2.2.0
updated: "2026-04-03"
---

# 创作足迹追踪专家（v2.2）
> 版本：2.2.0（2026-04-02）
> 来源：红龙公爵家ch001-030 92段完全重复事故 × 5类机械错误 × 文风一致性原理 × 联网扩充
> 定位：保护叙事新鲜度；重要性等同于novel-expert-system
> 配套：expert-memory（剧情逻辑）/ expert-pacing（节奏）/ expert-emotion（情感设计）

---

## 一、事故案例（活生生的教训）

### 1.1 红龙公爵家ch001-030 事故数据

| 指标 | 数值 |
|------|------|
| 受影响范围 | ch001-030（前30章/205章，占14.6%） |
| 完全重复段落数 | **92段** |
| 另有变体填充段落 | ~46段（夜色渐深万能结尾） |
| 填充字数 | **~7,820字** |
| 每段字数 | ~78-98字 |

### 1.2 五种模板段落详情

```
模板A【暮色场景】：暮色降临/远山剪影/壁炉余烬 → 出现24次
模板B【室内光影】：房间暗/烛火摇曳/古老气味 → 出现23次
模板C【户外夜景】：领地夜晚安静/月光/犬吠 → 出现22次
模板D【清晨薄雾】：清晨薄雾/农夫劳作/炊烟 → 出现22次
模板E【清晨苏醒】：清晨苏醒/铁匠铺/农田（变体） → 出现1次
```

### 1.3 事故根因：AI不知道自己前面写过什么

```
问题本质：模型生成时，上下文窗口中不一定包含前文已写的具体段落
→ AI每次生成都是"重新开始"
→ 低温度参数会让AI选择训练数据中出现频率最高的意象
→ "月光/烛火/炊烟" = AI的"舒适区"
→ 没有外部记录，AI无法意识到"这个段落已经写过了"
```

---

## 二、Footprints的真正目的：文风一致，不是逐字查重

### 2.1 逐字重复 vs 文风重复

```
❌ 逐字重复（严格禁止）：
  → 第3章："暮色降临，远山只剩剪影"
  → 第87章："暮色降临，远山只剩剪影"
  → 完全相同的句子，完全没有情节变化

✅ 文风重复（允许存在）：
  → 第3章："暮色降临，远山只剩剪影"
  → 第87章："黄昏的光线从天际消失，山脉的轮廓在暮色中变得模糊"
  → 同一氛围，不同的文字表达，文风一致

✅✅ 有意为之的风格标记（允许）：
  → 某个角色每次出现都有特定的描写手法
  → 某个重要场景每次出现都用相似的结构
  → 某个情感高潮前的场景描写有固定的模式
  → 这些是风格，不是错误
```

### 2.2 判定标准：删掉这段，读者会发现吗？

```
好的重复（可接受）：
  → 这段描写建立了某种氛围/风格感
  → 删掉后，熟悉作品的读者会说"少了点什么"
  → 有叙事功能（建立基调/暗示情绪/固定角色形象）

坏的重复（必须修改）：
  → 删掉后，读者完全不会注意到
  → 这段没有任何只有本文才有的独特元素
  → 任何一本中世纪小说都能用这段凑字数
```

### 2.3 footprints的范围弹性

```
严格范围（1~30章）：
  → 同一段落/场景逐字重复 → 禁止
  → 同一描写方向但不同文字 → 警告（换角度/感官/词汇）

适度放宽（30~50章）：
  → 同一意象可以再现，但必须有新的叙事功能
  → 再现时必须有变化：角度/词汇/感官类型/节奏至少改一样

宽松范围（50章以上）：
  → 可以有意复用同一意象/氛围
  → 复用时必须比前次更丰富
  → 没有任何叙事推进的纯粹复用 → 警告
```

### 2.4 例外：hentai.md亲密场景描写（可灵活）

```
当使用hentai.md中的描写技术时：
→ 允许更多的词汇密度（特定意象的出现频率可以放宽）
→ 允许类似结构在不同亲密场景中再现
→ 允许同类感官描写在手感/温度/声音等维度重复

但必须满足：
① 有真正的情节推进（不是同一句话换词）
② 每次出现都有新的细节层次
③ 同一意象在相邻章节内不重复出现
```

### 2.5 文风一致性三问（替代逐字查重）

```
问自己（不是"这段是否逐字重复"）：
① 这段是否符合本文当前的叙事声音？
② 这段是否会因为太"标准化"而破坏了本文的独特性？
③ 如果这段出现在任何一本中世纪小说里，那它是本文独有的吗？

三问全"否" → 修改
三问有一个"是" → 可以保留
```

---

## 三、五类机械错误（图谱）

### A类：无功能时间转场

```
表现：用"天色暗了/天亮了/夜深了"标记时间流逝，但与情节毫无关联
特征：天色变化 → 环境细节 → 感官描写，无论章节类型都插入
正确用法：时间变化跟角色心理挂钩，服务于情节
```

### B类：静态环境描写（与情节脱节）

```
表现：描写房间/领地/城堡的物理环境，与本章核心事件无呼应
特征：纯感官堆砌，视觉+听觉+嗅觉，无角色反应，无情节功能
正确用法：烘托情绪 / 暗示事件 / 折射角色状态
```

### C类：万能章节收尾段落

```
表现：章节末尾插入固定"夜晚独处"段落作为万能结尾
典型：夜色渐深 → 灯火熄灭 → 角色独坐 → 回顾 → 需要变强
正确用法：章节结尾必须有叙事功能：悬念/反转/新信息/情感高潮
```

### D类：感官堆砌式描写

```
表现：视觉→听觉→嗅觉→触觉机械罗列，形成"五感清单"
正确用法：选择1-2种最能服务于情节的感官重点（精准 > 全覆盖）
```

### E类：通用意象复用

```
表现：反复使用"安全牌"意象——月光、烛火、壁炉、炊烟、薄雾
频率数据（红龙公爵家ch001-205）：月光197次/远山111次/壁炉118次/烛火79次/薄雾39次/炊烟25次
正确用法：替换为本世界独有的设定元素（龙焰荧光/源力尘埃/红土气息）
```

---

## 四、四问自检法（判定标准）

| # | 问题 | 答案"否"=修改 |
|---|------|-------------|
| 1 | 这段描写是否只在本章出现？（全文唯一性） | ❌ 逐字重复，删除或改写 |
| 2 | 删掉这段后，本章情节是否受损？（叙事必要性） | ❌ 冗余，删除 |
| 3 | 这段描写是否跟本章核心事件有直接关联？（情节绑定性） | ❌ 脱节，重写 |
| 4 | 这段描写中是否有本世界独有的元素？（独特性） | ❌ 通用，替换为设定元素 |

```
四问全"是" → ✅ 合格
任一"否"   → ⚠️ 修改或删除
```

---

## 五、Footprints追踪分类体系

### 5.1 十大追踪类别

```
类别A【环境描写】：天气/光线/场景/气味（关联A类+B类机械错误）
类别B【战斗描写】：力量爆发/技能/受伤/氛围
类别C【人物高光时刻】：表情/动作/名场面
类别D【器物描写】：武器/服装/建筑/地图
类别E【情感时刻】：哭/心动/愤怒/告别的具体表达
类别F【对白手法】：标志性口头禅/特定情绪下的对白模式
类别G【情节装置】：反转/伏笔/揭露方式（用后标记"已耗尽"）
类别H【情绪渲染手法】：比喻/乐景写哀/沉默代替语言
类别I【力量体系专用】：境界突破感觉/能力视觉效果
类别J【转场手法】：时间转场/章节间过渡（关联C类）
```

### 5.2 freshness等级（新鲜度评级）

```
🍃 Green（新）：本项目第1-30章使用过 → 可以用，但必须改变角度/感官
🟡 Amber（适中）：第31-100章使用过 → 必须换词汇/节奏/感官重点
🟠 Dated（老化）：100章以上使用过 → 必须翻新
🔴 Red（耗尽）：同写法出现≥3次 → 立即加入禁止列表
```

### 5.3 🚫禁止列表（逐字重复严禁）

```
神王投影项目：
1. "金色的光芒" → 替换：金色气血的触觉/听觉表现
2. "力量像洪水一样倾泻" → 替换：对手视角/环境扭曲
3. "时间仿佛静止了" → 替换：具体感知变化
4. "她的眼眶红了" → 替换：动作/沉默/声音变化

通用网文（所有项目）：
1. 无情节绑定的"暮色降临"场景
2. 无情节绑定的"夜色渐深"万能章节结尾
3. 无情节绑定的"烛火摇曳"室内描写
4. 五感清单式描写（视觉/听觉/嗅觉逐项罗列）

hentai.md亲密场景例外：允许更高的词汇密度，允许同类结构再现
```

---

## 六、写作前检查流程

### 6.1 提示词强制指令（每次生成前必须加入）

```
【防重复强制指令】
1. 禁止逐字使用前文已出现的任何描写段落（逐句检查）
2. 禁止用"天色/月光/烛火/炊烟"等通用意象做无功能填充
3. 章节结尾禁止使用"夜色渐深"类万能收尾
4. 字数不足时扩展优先级：对话 > 心理 > 行动 > 世界观 > 场景
5. 每段描写必须通过四问自检
6. 环境描写必须与本章核心事件有直接叙事关联
```

### 6.2 三步检查流程

```
写前（15分钟）：
① 读取本项目.footprint.md
② 对照freshness等级和禁止列表
③ 有⚠️警告 → 找替代方案

写后（15分钟）：
④ 登记本章新元素到.footprint.md
⑤ 对照🚫禁止列表检查本章是否有违规
⑥ 有新元素 → 立即登记

批量后：
⑦ 批生成3-5章后，立即运行重复段落扫描脚本
⑧ 发现A/B/C类机械错误段落 → 立即替换或删除
```

---

## 七、检测脚本（复制即用）

### 7.1 grep扫描（单文件）

```bash
# 扫描指定章节中的5种机械错误
echo "=== A类：无功能时间转场 ==="
grep -n "天色渐渐暗了下来\|薄雾还没散尽\|夜色渐深" "$1"

echo "=== C类：万能收尾 ==="
grep -n "夜色渐深.*灯火.*熄灭\|夜幕降临.*灯火.*熄\|夜已深.*只有" "$1"

echo "=== E类：通用意象频率 ==="
for word in "月光" "烛火" "炊烟" "薄雾" "壁炉"; do
  count=$(grep -o "$word" "$1" | wc -l)
  [ "$count" -gt 1 ] && echo "⚠️ $word 出现 $count 次"
done
```

### 7.2 Python批量扫描（整项目）

```python
import re, os
from collections import defaultdict

def scan_project(folder):
    files = sorted([f for f in os.listdir(folder) if f.endswith('.txt')])
    all_text = {}
    for fname in files:
        path = os.path.join(folder, fname)
        with open(path) as f:
            text = f.read()
        all_text[fname] = text

    # 扫描C类万能结尾
    print("=== C类万能结尾扫描 ===")
    patterns = [
        "夜色渐深", "夜幕降临", "夜已深",
        "灯火.*熄灭", "灯火阑珊", "夜深人静",
        "独坐窗前", "月色.*窗", "唯有.*陪伴"
    ]
    for fname, text in all_text.items():
        for p in patterns:
            if re.search(p, text):
                print(f"⚠️ {fname}: 发现C类变体「{p}」")

    # 扫描E类通用意象频率
    print("\n=== E类通用意象频率统计 ===")
    generic = {"月光":0,"烛火":0,"炊烟":0,"薄雾":0,"壁炉":0}
    for fname, text in all_text.items():
        for word in generic:
            cnt = len(re.findall(word, text))
            if cnt:
                generic[word] += cnt
    for word, cnt in sorted(generic.items(), key=lambda x:-x[1]):
        if cnt > 0:
            level = "🔴" if cnt>100 else "🟠" if cnt>30 else "🟡"
            print(f"{level} {word}: {cnt}次")

    # 扫描连续相同段落（≥3句）
    print("\n=== 逐字重复段落扫描 ===")
    from collections import Counter
    all_paras = []
    for fname, text in all_text.items():
        paras = re.findall(r'(?:[^\n]{20,}\n){2,}[^\n]{20,}', text)
        for p in paras:
            all_paras.append((fname, p.strip()[:80]))
    counter = Counter([p for _,p in all_paras])
    for para, cnt in counter.most_common(10):
        if cnt > 1:
            print(f"🔴 出现{cnt}次: {para[:60]}...")

# 示例用法
scan_project('/workspace/长篇小说及规划/穿越异界_神王投影/卷一/')
```

---

## 八、C类万能结尾二次清理SOP

> 发现：46处C类结尾首次清理漏删20处 → 必须二次扫描

```
标准流程：
Step 1：初检（grep批量扫描）
Step 2：初检后立即人工抽查3-5章（章节首中尾三处）
Step 3：二检（24小时内用不同关键词再扫一次）
Step 4：标记确认（将已清理标记写入footprint）
```

### C类万能结尾常见变体关键词

```
夜色渐深/夜幕降临/夜已深/夜色笼罩
灯火.*熄灭/灯火阑珊/灯火渐熄
夜深人静/独坐窗前/月光洒在/月色.*窗
黑暗中.*只有/唯有.*陪伴
```

---

## 九、字数统计口径标准

### 三套统计标准

| 标准 | 计算方式 | ch32字数 | 适用 |
|------|---------|---------|------|
| **窄口径（推荐）** | 中文+中文标点 | ~2072字 | 项目通用 |
| 宽口径 | 所有非空白字符 | ~2165字 | 快速估算 |
| 纯中文 | 仅中文 | ~1862字 | 精确字数 |

```
宽口径 vs 窄口径差异：全书约86章（42%）
→ 项目启动时必须锁定统计口径，建议用窄口径
```

### 字数警告触发规则

```
正常：≥2100字（窄口径）
⚠️  警告：1800-2099字
❌  不足：1500-1799字 → 必须扩充
🚨  异常：<1500字 → 人工检查正文是否损坏
🚨  疑似损坏：<500字 → 标记⚠️（ch112/ch121/ch133/ch144）
```

---

## 十、扩充优先级体系

| 优先级 | 手法 | 说明 |
|-------|------|------|
| 🥇 最高 | **扩展对话** | 直接增加互动，信息量大 |
| 🥈 次高 | **增加心理活动** | 深化代入感 |
| 🥉 第三 | **丰富战斗/行动细节** | 提升画面感 |
| 第四 | **补充世界观信息** | 增加厚度 |
| 第五 | **有功能的环境描写** | 仅在必要时候用 |
| ❌ 禁用 | **无功能的场景填充** | A/B/C类机械错误 |

```
❌ 错误：硬指标"每章≥2100字" → AI为达标而填充
✅ 正确：范围指标"2000-2800字" → 允许自然波动
```

---

## 十一、Footprints文件格式

### 命名规范

```
/workspace/长篇小说及规划/[项目名]/.footprint.md
例：/workspace/长篇小说及规划/穿越异界_神王投影/.footprint.md
```

### 标准格式模板

```markdown
# 【FOOTPRINT】[项目名] 创作足迹追踪
> 建立时间：YYYY-MM-DD
> 版本：v2.2（文风一致性原则）

---

## 🚫 禁止列表

```
[逐字重复的写法 + 替代方案]
```

---

## 🍃 类别A：环境描写

| 写法 | 章节 | freshness | 替代方案 |
|-----|------|---------|---------|
| ... | ... | 🟠 Dated | 换成触感/听觉 |

---

## 四问自检法

```
① 删掉后读者会发现吗？（叙事必要性）
② 跟本章核心事件有关联吗？（情节绑定性）
③ 有本世界独有元素吗？（独特性）
④ 这段符合本文的叙事声音吗？（文风一致性）
```

---

## 写作前检查清单

```
☐ 读取本项目.footprint.md
☐ 对照禁止列表和freshness警告
☐ 写完后运行扫描脚本
☐ 登记本章新元素
☐ hentai.md场景 → 记录，规则可灵活
```

---

## 创作足迹追踪技法演进

### 一、文风一致性检查方法

```
方法一：关键词频率分析
  统计高频词汇/句式/修辞手法
  如果某个词汇/句式出现频率异常高 → 可能是模板化写作

方法二：场景描写重复检测
  同一场景在不同章节的描写不应逐字重复
  例：打斗描写不能每次都是"拳风呼啸，劲气四溢"

方法三：角色语言指纹检查
  每个角色应有独特的说话风格
  如果所有角色说话方式一样 → 文风单一
```

### 二、freshness评级系统

```
评级标准：
  S级（新鲜）= 完全原创，无重复模式
  A级（良好）= 基本原创，偶有相似
  B级（一般）= 部分模式化，但不影响阅读
  C级（需改）= 明显模板化，需要修改
  D级（重写）= 严重重复，需要重写

检查频率：
  每5章做一次freshness评级
  连续3章C级 → 需要暂停调整文风
  出现D级 → 立即重写该章节
```

### 三、强制禁止列表

```
禁止的写作模式：
❌ 同一章节内重复使用同一比喻
❌ 连续3章使用相同类型的开篇（都是对话/都是场景描写）
❌ 所有战斗场景用同一套描写模板
❌ 所有角色使用相同的口头禅
❌ 章末钩子总是同一类型（总是悬念/总是反转）
❌ 人物出场总是先描写外貌再描写性格

例外：
  系列化写法中，固定的"仪式性重复"是允许的
  例：每章开头的经典台词/固定的场景描写（属于品牌标识）
```


---

## 经典技法补全

> 本章节补充可直接套用的经典技法模板，供创作者在实际写作中参考。

### 10.1 痕迹密度控制公式

"剧情痕迹"是指作者在文本中留下的、读者可以追踪的信息线索。痕迹太稀疏，读者抓不住叙事逻辑；痕迹太密集，读者觉得被过度引导。痕迹密度控制公式帮助作者找到最佳平衡点。

```
痕迹密度分类：

核心痕迹（高密度，1:3~1:5章）
  → 与当前卷/当前主线直接相关的信息
  → 每章至少有1-2个痕迹元素
  → 例：当前正在调查的案件线索

支撑痕迹（中密度，1:10~1:15章）
  → 支撑世界观/角色背景的间接信息
  → 不需要每章出现，但要定期刷新
  → 例：角色的过去经历/世界的隐藏规则

远景痕迹（低密度，1:30~1:50章）
  → 全书级的宏观线索/最终真相的碎片
  → 越到后期密度逐渐增加
  → 例：最终Boss的真正目的/世界毁灭的根源

密度递增法则：
  前期（1-30章）：远景痕迹每50章1次
  中期（30-100章）：远景痕迹每20章1次
  后期（100章+）：远景痕迹每5-10章1次
  → 前期稀疏是为了不让读者过早猜到结局
  → 后期密集是为了让回收有充足铺垫
```

### 10.2 读者预期管理四步法

读者在阅读过程中会不断形成预期——"接下来会这样发展"。高明的作者既利用预期制造满足感（读者猜对了的成就感），又打破预期制造惊喜感（读者没猜到的新鲜感）。四步法帮助作者管理这个过程。

```
① 建立预期（引导读者形成判断）
  手法：通过正常的情节发展让读者觉得"应该是这样"
  例：主角遇到了一个神秘的老人 → 读者预期"这是传授功法的NPC"
  → 不需要刻意引导，正常的叙事就会产生预期

② 巩固预期（让读者确信自己的判断）
  手法：给出1-2个"印证"读者预期的线索
  例：老人确实展示了超凡实力 → 读者更加确信"果然是传功NPC"
  → 巩固是为了让后来的反转更有冲击力

③ 颠覆预期（给出超出预期的真相）
  手法：揭示读者判断中遗漏的关键信息
  例：老人其实是最终Boss的化身，传授功法是为了植入追踪印记
  → 颠覆不能是"凭空出现的新信息"，必须是"之前就存在但被忽略的线索"

④ 合理化颠覆（让读者接受新真相）
  手法：通过回溯/闪回展示"被忽略的线索"
  例："回想起来，老人的笑容中总有一丝不易察觉的……"
  → 合理化要快（1-2段话），不能让读者觉得作者在补设定
```

### 10.3 信息差编排矩阵

信息差是制造悬念和戏剧冲突的核心工具。不同角色知道不同信息，读者知道的信息量也与角色不同。信息差编排矩阵帮助作者管理"谁知道什么"，确保信息差产生最大的戏剧效果。

```
信息差矩阵模板：

                读者   主角   配角A   反派
真相A（Boss身份） 知道   不知道  不知道  知道
真相B（宝物位置） 不知道  知道   不知道  知道
真相C（身世之谜） 不知道  不知道  知道   知道

三种经典信息差模式：

模式1：读者 > 角色（上帝视角紧张感）
  读者知道危险，角色不知道
  → 产生"快跑啊！"的紧张感
  → 适用于：悬疑、恐怖、灾难铺垫

模式2：角色 > 读者（揭秘快感）
  角色知道真相，读者不知道
  → 产生"到底是什么？"的好奇心
  → 适用于：推理、智斗、反转

模式3：角色之间信息不对称（冲突根源）
  角色A知道X但不知道Y，角色B知道Y但不知道X
  → 产生误解/冲突/合作
  → 适用于：人际关系、势力博弈、多视角叙事

编排铁律：
  每5-8章必须有一次"信息差消除"（否则读者会烦躁）
  信息差消除的时机 = 情绪最高点
  信息差消除的方式 = 要有戏剧冲突（不是平静地"告诉"）
```

### 10.4 自检清单

- [ ] 三类痕迹（核心/支撑/远景）是否采用了不同密度？
- [ ] 远景痕迹是否遵循了"前期稀疏、后期密集"的递增法则？
- [ ] 读者预期管理是否完成了四步（建立→巩固→颠覆→合理化）？
- [ ] 颠覆预期时是否有前期线索支撑（非凭空出现）？
- [ ] 信息差矩阵是否清晰标注了"谁知道什么"？
- [ ] 每5-8章是否安排了一次信息差消除？
- [ ] 是否避免了同一类型的痕迹连续出现？
